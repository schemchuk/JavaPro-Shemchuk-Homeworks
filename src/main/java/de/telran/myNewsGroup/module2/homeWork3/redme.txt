 1 уровень сложности: 1. Реализовать модель действий работников в офисе:
    a) Создать интерфейс JobActions
public interface JobActions {
    void doTask(Task task);
    void haveRest();
}
    b) В интерфейс JobActions поместить класс Task с описанием задач
class Task {
    private String name;
    private String description;
}
В классе Office в main() методе
    c) создать несколько задач Task
    d) через анонимные классы имплементирующие JobActions создать несколько работников:
        manager - сообщает название заданий, пьет кофе по время отдыха
        worker - делает задания, курит по время отдыха
        lazyWorker - все время бездействует
    e) передать задачи работникам и проверить работу методов классов


Для списка Employee написать методы getFirstByName(list), getLastByName(list), которые получали бы первого и последнего по имени сотрудника.

Для списка Employee реализовать
а) сортировку по имени в обратном порядке z-a
b) сортировку по рабочим часам и возрасту в обратном порядке